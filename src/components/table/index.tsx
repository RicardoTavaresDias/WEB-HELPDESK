import { PanelColumn } from "./panelColumn";
import { PanelRoot } from "./PanelRoot";
import { PanelRows } from "./panelRows";

export const Panel = {
  Root: PanelRoot,
  Column: PanelColumn,
  Rows: PanelRows
}